let gpu = new GPU();



let runLayer = gpu.createKernel(function (input, weights, biases,inputSize, outputSize) {
    let sum = 0;
    
}, {
    output: [5],
    dynamicOutput: true,
});
























